# Local Dev Ports

Run `./scripts/dev.sh --api-port PORT` to start the dev stack.
This file will be updated with your actual ports on startup.

This is an API-only (MCP server) template — there is no Vite SPA.
